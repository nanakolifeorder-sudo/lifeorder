整合驗收與後台收斂報告
日期：2026-08-28

一、本次驗收目標

1. 從 /quiz 作答。
2. 送出後進入 /report。
3. 點擊報告頁預約 CTA。
4. 進入 /booking。
5. 完成預約。
6. 回 CRM 檢查作答、分數、報告、預約、Email、點擊歷程是否串起來。
7. 做後台 UI 最終整理：左側欄、分類命名、測驗設定 tabs、CRM 批次操作與文案。

二、production 黑箱驗收結果

測試環境：
- 網址：https://dm-booking-saas.vercel.app
- tenant：dm-test
- project：P01
- 測試信箱：full-e2e-1787882775639@example.com

已通過：
- getQuizConfig 可讀到 P01 / A 版測驗。
- 測驗題目數：4 題。
- 分數面向數：3 個。
- submitQuiz 可成功建立 quiz_response。
- scoreSummary 有 3 個面向分數。
- reportSnapshot 有 3 個報告區塊。
- getQuizResult 可讀取報告。
- trackReportClick 可記錄預約 CTA 點擊。
- getAvailableTimes 可讀到 70 個可預約測試時段。
- submitBooking 可成功建立預約。
- CRM contact lifecycle_stage 已更新為 booked。
- CRM contact latest_quiz_response_id、latest_appointment_id、assigned_consultant_id 已回填。
- CRM timeline 可查到 quiz_completed、quiz_report_viewed、booking_cta_click、booking_created。
- appointments 已建立預約 id 312，顧問為 DMtest 顧問，時間為 2026-08-29 10:00 Asia/Taipei。

本次產生的報告連結：
https://dm-booking-saas.vercel.app/report?tenant=dm-test&p=P01&qrid=16efc8d0-268f-4fc4-bd8a-66f966e531ea

本次補洞：
- dm-test 顧問尚未連 Google Calendar，新增只限 dm-test 的 internal_test booking 模式，讓內部測試可以完成預約資料流。
- 修正 availability specific date 的時區比對，避免 date_value 以 UTC 儲存後無法對到 Asia/Taipei 日期。
- 已補 dm-test 顧問 id 4 的 2026-08-29 到 2026-09-07 測試時段，每天 10:00-17:00。

三、Email / OAuth 驗收結果

已補 P01 內部測試 Email 模板：
- quiz_completed：測驗完成通知。
- quiz_completed_unbooked：未預約跟進。
- booking_created：預約成功通知。
- meeting_before：會議前提醒。

production E2E 已驗證：
- 測驗送出後會建立 2 筆 quiz module queue。
- 完成預約後，quiz_completed 與 quiz_completed_unbooked 會被取消。
- 完成預約後會新增 booking_created 與 meeting_before 兩筆 booking module queue。
- booking module queue 已正確帶 appointment_id 與 quiz_response_id。
- CRM timeline 可看到測驗完成、報告查看、CTA 點擊、預約建立、信件排入佇列。

正式寄信注意事項：
- dm-test 目前沒有任何顧問或管理員連接 Google OAuth sender，因此 queue 可以建立，但不能正式寄出 Gmail。
- 正式上線前需要由管理員完成 Gmail OAuth，或指定可寄信的系統寄件帳號。

四、目前注意事項

internal_test booking 只應用在 tenant = dm-test：
- 正式 tenant 仍維持原規則，顧問必須連接 Google Calendar 或 Microsoft Calendar 才會產生可預約時段。
- internal_test 預約會建立 appointments 與 CRM timeline，但不會真的建立 Google Calendar event。

五、本次後台 UI 收斂內容

已整理：
- 左側功能列寬度與間距縮小，降低佔用畫面。
- 左側導覽分類名稱更貼近正式規劃。
- 客戶管理：客戶 CRM、作答數據。
- 預約系統：預約表單、排班系統、顧問管理、提醒信管理。
- 測驗系統：題目管理、檢測報告設定、跟進信管理。
- 系統設定：專案管理、系統用量、帳號 / 權限管理。
- Email 後台入口拆成提醒信管理與跟進信管理，但底層仍共用 email_templates / email_queue。
- CRM 批次操作列改成更緊湊的排列。
- CRM 批次操作文案改成「替選取名單加標籤」、「替選取名單改狀態」、「套用」。
- 測驗設定上方 tabs 增加分隔線，讓目前編輯區與切換區更清楚。

六、目前結論

測驗漏斗到 CRM 的主資料鏈已成立：
/quiz 作答 -> /report 報告 -> CTA 點擊追蹤 -> CRM 資料卡歷程。

完整測驗到預約到 CRM 的 production 主鏈路已跑通。剩下正式上線前必補的是 Gmail / Calendar OAuth 實際授權，以及後台 UI 的細部驗收調整。
七、本階段 OAuth / Email / 後台 UI 收斂紀錄

執行時間：2026-08-28

1. Google OAuth 正式串接狀態
- 程式端已具備 Google OAuth URL、callback、refresh token 儲存、Calendar busy 查詢、Calendar event 建立、Google Meet 產生與 Gmail send scope。
- dm-test 目前所有顧問與管理員 google_refresh_token 都是空值，因此還不能完成「真實 Gmail 寄出」與「真實 Google Calendar event / Meet 建立」。
- 後台已提供「連結 Google Calendar / Meet / Gmail」按鈕；正式驗收需要管理員或顧問本人登入後點擊授權。
- Zoom 目前仍只保留規格與選項方向，尚未進入 Zoom OAuth/API 串接階段。

2. 正式寄信驗收狀態
- P01 內部測試模板已建立：測驗完成通知、未預約跟進、預約成功通知、會議前提醒。
- production E2E 已驗證 queue 流程：測驗後排測驗信，預約後取消未預約跟進信，再排預約相關信件。
- booking email queue 已帶 appointment_id 與 quiz_response_id。
- Gmail 實寄尚未驗收，原因是 dm-test 尚無已授權 Google sender。

3. 後台 UI 收斂
- 左側功能列寬度、收合寬度、群組間距、按鈕圖示尺寸縮小。
- CRM 篩選文案改為「標籤」、「名單狀態」、「搜尋名單」。
- CRM 統計文案改為「今日作答」、「今日預約」、「近期預約」。
- CRM 名單狀態 chip 改為「近期預約」、「待追蹤名單」、「已完成/過期」、「取消/婉拒」。
- CRM 批次操作文案改為「全選目前結果」、「取消選取」、「替已選名單加標籤」、「替已選名單改狀態」、「套用到已選名單」。
- 客戶卡片欄位文案改為「下一步」、「負責人」、「專案」、「名單狀態」；資料卡按鈕改為「打開資料卡」。
- 測驗設定頁主標改為「題目管理」，上方 tabs 維持橫向功能列，匯入按鈕文案改為「下載格式檔」、「匯入並預覽」。

下一個阻塞點：
- 需要真人登入後台並完成 Google OAuth 授權，才能繼續驗證 Gmail 實際寄出、Google Calendar busy 查詢、真實 Calendar event 與 Google Meet 連結。